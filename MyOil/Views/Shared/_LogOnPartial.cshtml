@if (Request.IsAuthenticated)
{ 
<text>欢迎<strong>@User.Identity.Name</strong>![@Html.ActionLink("注销", "LogOut","Account")]</text>
}
else
{
    @:[ @Html.ActionLink("登录", "LogOn", "Account") ]
}